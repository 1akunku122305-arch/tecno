// ---------------------------------------------------------------------------
// Server client factory that returns a typed SupabaseClient for service calls.
// ---------------------------------------------------------------------------
export { createClient as createServerSupabase } from "./server";
export { createClientBrowser } from "./client";
